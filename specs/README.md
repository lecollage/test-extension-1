# Specs

This directory stores specs for non-trivial work.

Each spec should live in:

- `specs/<feature-slug>/spec.md`
- `specs/<feature-slug>/tasks.md`
- `specs/<feature-slug>/notes.md`

Create a new spec with:

```bash
npm run spec:new -- <feature-name>
```

Templates live in `specs/templates/`.
